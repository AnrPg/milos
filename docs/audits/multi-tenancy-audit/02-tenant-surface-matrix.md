# Tenant Surface Matrix

Date: 2026-08-05. Status legend: **OK** = correctly enforced (app predicate
and/or RLS, verified) · **GAP** = confirmed missing/weak enforcement ·
**FUNCTIONAL-BREAK** = fails closed (no leak) but likely non-functional for
non-legacy tenants · **UNVERIFIED** = reported by delegated research, not
independently re-read line-by-line during this pass.

| Surface | Entity/operation | Tenant source | Read scoped | Write scoped | DB enforced (RLS) | Cache/job/file scoped | Status | Evidence |
|---|---|---|---|---|---|---|---|---|
| Finance | Invoices/payments/packages/promotions/referrals via `admin_finance_controller.ex` | `conn.assigns.tenant_context` wrapped around whole controller action | Yes | Yes | Yes (force, w/ legacy COALESCE fallback — F-04) | N/A | **OK** (but see F-04/F-05 for the fallback risk) | `admin_finance_controller.ex`, `ecto_finance_store.ex` |
| Finance | `Finance.get_invoice/1` (1-arity delegate) | None — unscoped | No (relies on RLS + manual post-check at 2 call sites) | N/A | Yes (w/ fallback) | N/A | **GAP** | F-05, `finance.ex:138` |
| Finance | `financial_summary/1` (`finance_aggregates` MV) | None | **No** | N/A | **N/A — materialized view, no RLS possible** | N/A | **GAP — Critical, confirmed live leak** | F-21 |
| Finance | `mark_overdue_invoices`/`payment_reminder`/`reconcile_entitlement_reservations` Oban jobs | None — no `RepoContext.run` | N/A | No | Yes (fallback routes to legacy org only) | Job args carry no `organization_id` | **FUNCTIONAL-BREAK** | F-24 (unverified — flagged for follow-up read) |
| Scheduling | Classes/bookings/attendance via `schedule_controller.ex`/`admin_schedule_controller.ex` | `conn.assigns.tenant_context` per-call | Yes | Yes | Yes (force) | N/A | **OK** | Confirmed: tests pass (`scheduling/tenant_isolation_test.exs`), `scheduling_store.ex:8-11` |
| Scheduling | Legacy no-context store arities | `legacy_scope/0` (server-controlled) | N/A | N/A | Yes | N/A | **OK, but legacy-only** (F-18 — functional inequality, not a leak) | `scheduling_store.ex:46-53` |
| Workouts | Master workouts, folders, assignments | Mostly `scoped_to_tenant()`, some `Repo.get` without | Partial | Partial | Yes (force) | N/A | **PARTIAL — recommend follow-up read** | `ecto_workout_store.ex` (multiple unscoped `Repo.get` call sites flagged by delegated research, not all independently confirmed) |
| Execution | Member-facing start/list/get | `conn.assigns.user_context` | Yes | Yes | Yes (force, w/ user+org+admin_mode+auth-check branches) | N/A | **OK for non-admin path** | `execution_controller.ex`, `owner_isolation_test.exs` passes |
| Execution | `start_execution` for `self_selected`/`class_booking` sources | **Client-supplied `organization_id`** | N/A | **No** | Yes, but provenance value itself is unvalidated | N/A | **GAP** | F-03 |
| Execution | Admin `GET /api/executions/:id` | `context.account.role == "admin"` (global role) → `admin_mode` RLS bypass | **No — org-blind** | N/A | Bypassed by design (`admin_mode`) | N/A | **GAP — Critical, confirmed IDOR** | F-23 |
| Execution | `ProcessWorkoutCompletionJob` | None | N/A | N/A | Blocks (returns nil) | Job args carry ids but unused for context | **FUNCTIONAL-BREAK (unverified)** | F-26 |
| Gamification | Challenges/opt-ins/settings | `scoped_to_tenant()` | Yes | Yes | Yes (force) | N/A | **OK** | `ecto_gamification_store.ex`, `gamification/tenant_isolation_test.exs` passes |
| Gamification | `get_leaderboard/2` (`weekly_leaderboard` MV) | None | **No** | N/A | **N/A — materialized view** | N/A | **GAP — Critical, confirmed live leak, no auth gate at all** | F-22 |
| Coaching | Athlete drill-down | `conn.assigns.tenant_context` + explicit `active_athlete_membership?/2` check | Yes | N/A (read-only projection) | N/A for `coaching_aggregates` (app-filtered correctly) | N/A | **OK — best-practice example in the codebase** | `get_coaching_athlete_drill_down.ex`, `ecto_coaching_store.ex:8-22` |
| Notifications | User notification CRUD, push subscriptions | `conn.assigns[:user_context]` | Yes | Yes | Yes (owner, force) | Job scoping correct (`notification_event_job.ex` sets `RepoContext.run`) | **OK** | `notification_controller.ex`, `notification_event_job.ex` — **but no dedicated tenant_isolation_test exists (F-14)** |
| Messaging | Threads/messages | **None — Application layer never opens tenant context** | No (fails closed) | No (fails closed) | Yes (force, fallback to legacy) | N/A | **FUNCTIONAL-BREAK** | F-25 (unverified — flagged for follow-up read) |
| Analytics | Admin member search (Meilisearch) | App-layer post-filter only, index itself unfiltered | Yes (via post-filter, single call site) | N/A | N/A (Meilisearch, no RLS) | **Fragile — one call site protects the whole surface** | **GAP (structural fragility)** | F-27 (unverified) |
| Analytics | PR search (Meilisearch) | Index-level `filter: "user_id = ..."` | Yes | N/A | N/A | Correctly scoped at index layer | **OK — best-practice contrast to member index** | `meilisearch_pr_index.ex`, `sync_pr_search_job.ex` |
| Feedback | Reviews/questionnaires/answers | Controller-level `with_tenant_context` wrap | Yes | Yes | Yes (force) | N/A | **OK** | `admin_review_controller.ex`, `feedback/tenant_isolation_test.exs` passes — **no dedicated isolation test found for the legacy `review_controller.ex` fallback branch specifically** |
| Wellbeing | Injury reports (self + org-authored) | `conn.assigns.user_context`/`tenant_context` per action | Yes | Yes | Yes (force, dual owner-or-tenant policy) | N/A | **OK — correctly splits authorship per ADR-060** | `wellbeing_controller.ex`, `admin_wellbeing_controller.ex` — **no dedicated tenant_isolation_test exists (F-14)** |
| Pantheon (PRs) | User PR records/history | `user_id` threaded explicitly, fetch-then-check-owner pattern | Yes | Yes | Yes (force, owner) | Search index correctly scoped | **OK — best-practice IDOR defense** | `update_pr.ex`, `delete_pr.ex` |
| Identity | `users.role` global field | N/A (global, not tenant) | N/A | Gated by `:admin_only` tenant pipeline (inconsistently — the field itself is global) | N/A (`users` table RLS defined but never enabled — F-08) | N/A | **GAP** | F-02, F-08 |
| Organizations | Memberships, invitations, tenant root | Platform-owner/server-derived | Yes | Yes, except invitation role ceiling (F-06) | **No RLS on root tables at all** (F-16) | N/A | **OK for isolation, GAP for privilege-ceiling and defense-in-depth** | F-06, F-16 |
| Cross-cutting | HTTP tenant resolution for ~140 `/api/admin/*` routes | `x-organization-slug` header or legacy-org fallback | Membership-gated but header/legacy sourced | Same | N/A (upstream of DB layer) | N/A | **GAP** | F-01 |
| Cross-cutting | WebSocket tenant resolution without explicit param | Legacy-org fallback, membership-gated | Same | N/A | N/A | Channel topics correctly per-org once resolved | **GAP (legacy default), OK (isolation once resolved)** | `05-legacy-gym-inventory.md` §3 |
| Cross-cutting | Frontend `/admin/*` console | `localStorage` + JWT-first-membership (REST) vs JWT-first-membership only (WebSocket) — two disagreeing chains | UI-only; server re-validates membership | UI-only | N/A (frontend) | Query cache **not** org-scoped, no invalidation on switch | **GAP** | F-09 |
| Cross-cutting | Realtime broadcast helpers on missing `organization_id` payload field | Silent fallback to legacy org | N/A | N/A | N/A | Topic-scoped once resolved, but resolves to wrong org silently | **GAP (silent failure mode)** | F-19 |
| Cross-cutting | Test/CI database role | `postgres` superuser (bypasses RLS) in dev/test/CI; `milos_runtime` (correct, non-superuser) in `docker-compose.yml`'s `api` service | N/A | N/A | **RLS never actually exercised by any test** | N/A | **GAP — verification integrity** | F-07 |
